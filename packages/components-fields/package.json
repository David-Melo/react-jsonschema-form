{
  "name": "@react-schema-form/components-fields",
  "version": "1.0.0",
  "description": "React Schema Form fields (React components)",
  "main": "build/index.js",
  "module": "lib/index.js",
  "variable": "ReactSchemaFormFields",
  "license": "MIT",
  "scripts": {
    "start": "cross-env NODE_ENV=development babel src --out-dir lib --watch",
    "build:lib": "cross-env NODE_ENV=production babel src --out-dir lib",
    "build:umd": "neutrino build --use ../../.neutrinorc.lib.js",
    "build": "npm run build:lib && npm run build:umd"
  },
  "peerDependencies": {
    "react": "^16.4.0"
  },
  "dependencies": {
    "prop-types": "^15.6.1"
  },
  "devDependencies": {
    "react": "^16.4.0"
  }
}
